# Local development (app runs on the host, Postgres in a container)
DATABASE_URL=postgresql+psycopg2://user:password@localhost:5432/dbname

# Used by docker compose to configure the database container
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=dbname
